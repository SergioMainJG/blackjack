{
  "name": "blackjack",
  "private": true,
  "version": "alpha-1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 8080",
    "build": "tsc && vite build --outDir dist/ --minify esbuild",
    "preview": "vite preview",
    "optimize": "vite optimize --force"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^6.0.1"
  }
}
